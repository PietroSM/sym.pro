<form method="post" action="{{ path('app_habitacio_delete', {'id': habitacio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ habitacio.id) }}">
    <button class="btn">Delete</button>
</form>
